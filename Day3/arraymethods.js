const arr=[1,2,3,4,5]

console.log(`The array elements are ${arr}`);

console.log(`the lenght of the array ${arr.length}`);

const new_arr = arr.slice(1,4)
console.log(new_arr);

console.log(arr.join("-"));